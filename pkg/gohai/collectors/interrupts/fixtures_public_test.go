package interrupts_test

// Values these tests build fixtures from, named because each is used in
// four or more places.
const (
	irqIOAPIC          = "IO-APIC"
	osLinux            = "linux"
	pathProc           = "/proc"
	pathProcInterrupts = "/proc/interrupts"
)
