package selinux_test

// Values these tests build fixtures from, named because each is used in
// four or more places.
const (
	osLinux         = "linux"
	selinuxTargeted = "targeted"
	stateEnabled    = "enabled"
	value33         = "33"
)
