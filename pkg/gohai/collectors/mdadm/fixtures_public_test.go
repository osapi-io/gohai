package mdadm_test

// Values these tests build fixtures from, named because each is used in
// four or more places.
const (
	osLinux        = "linux"
	pathProc       = "/proc"
	pathProcMdstat = "/proc/mdstat"
	sda1           = "sda1"
	sdb1           = "sdb1"
)
