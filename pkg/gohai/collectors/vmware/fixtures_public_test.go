package vmware_test

// Values these tests build fixtures from, named because each is used in
// four or more places.
const (
	osDarwin         = "darwin"
	osLinux          = "linux"
	pathProcScsi     = "/proc/scsi"
	pathProcScsiScsi = "/proc/scsi/scsi"
	virtVMware       = "vmware_desktop"
)
